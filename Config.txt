1. Change cors
For deployment
app.use(cors({
  origin: "https://vlabsolutions-ddva.vercel.app/",
  methods: ["GET", "POST", "PUT"],  // Change the semicolon to a comma here
}));
For local host
app.use(cors({
  origin: "*",
  methods: ["GET", "POST", "PUT"],  // Change the semicolon to a comma here
}));